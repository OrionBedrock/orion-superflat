name: CI

on:
  push:
    branches: [development, main]
  pull_request:
    branches: [development, main]

jobs:
  build-test-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: plugin
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4
        with:
          path: plugin

      - name: Checkout OrionServerBE
        uses: actions/checkout@v4
        with:
          repository: OrionBedrock/OrionServerBE
          ref: development
          path: OrionServerBE

      - name: Checkout orion-minimal-items
        uses: actions/checkout@v4
        with:
          repository: OrionBedrock/orion-minimal-items
          ref: development
          path: deps/orion-minimal-items

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Pack Orion SDK from server checkout
        run: bash scripts/ci-setup-server-sdk.sh

      - name: Build orion-minimal-items
        working-directory: deps/orion-minimal-items
        run: |
          rm -rf "$HOME/.cache/orion-plugin-build"
          dotnet restore OrionMinimalItems.csproj --configfile "$GITHUB_WORKSPACE/plugin/nuget.config" --force-evaluate
          dotnet build OrionMinimalItems.csproj -c Release --configfile "$GITHUB_WORKSPACE/plugin/nuget.config" --no-restore

      - name: Build plugin
        run: |
          rm -rf "$HOME/.cache/orion-plugin-build"
          dotnet restore OrionSuperflat.csproj --configfile nuget.config --force-evaluate
          dotnet build OrionSuperflat.csproj -c Release --configfile nuget.config --no-restore

      - name: Unit tests
        run: |
          dotnet restore tests/Orion.Plugins.Superflat.Tests/Orion.Plugins.Superflat.Tests.csproj --configfile nuget.config --force-evaluate
          dotnet test tests/Orion.Plugins.Superflat.Tests/Orion.Plugins.Superflat.Tests.csproj -c Release --no-restore --verbosity minimal

      - name: Smoke boot server with plugin
        env:
          PLUGIN_DEP_DIRS: ${{ github.workspace }}/deps/orion-minimal-items
        run: bash scripts/ci-smoke-server.sh
